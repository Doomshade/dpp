struct File {
    const pp -> fd,
}

struct FileDef {
    struct File -> next,
    struct File -> prev,
}

FUNc main() -> pp {
    const struct File -> root = File {
        68,
    };
    root.fd = 123456;
    struct File -> transform_fd = transform_fd(root);
    bye transform_fd.fd;
}

FUNc transform_fd(struct File -> root,) -> struct File {
    bye File {
        root.fd + 1
    };
}
