struct Test {
    pp -> a,
    pp -> b,
    pp -> c,
}

FUNc main() -> pp {
    pp -> x;
    pp -> c;
    pp -> cx;
    pp -> ca;
    pp -> cb;
    pp -> cc;
    pp -> cd;
    struct Test[10] -> a;
    for(i to 10) {
        a[i] = Test{
        3 * i + 1,
        3 * i + 2,
        3 * i + 3,
        };
    }
    struct Test -> last = a[0];
    bye last.c;
}

FUNc test(pp -> a) -> nopp {
}
