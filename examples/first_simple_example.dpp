let x: pp;
let y: pp = 5;

FUNc test(aa: pp, bb: pp): pp {
    let test: pp = (1 + 2) * 3;
    let testasd: pp;
    abcd(123 > 2, test(yem) < 1);
    {
        {
            let a: pp;
        }
    }
    testasd = 123;
    testasd = test + 4 * 2 - 1;

    if (123) {
        let test: pp = yem;
    }
}

FUNc hellyea(aa: pp, bb: pp): pp {
    let yup: pp = (1 + 2) * 3;
    let nop: pp;
    yup = 123;
    nop = yup + 4 * 2 - 123;

    if (123) {
        let test: pp = 123;
    }
}
